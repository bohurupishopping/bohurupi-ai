name: bohurupi
description: Bohurupi Mobile App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  
# Firebase dependencies
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  google_sign_in: ^6.2.1
  cloud_firestore: ^5.4.4
  
  # Environment Variables
  flutter_dotenv: ^5.1.0
  
  # HTTP and Date Formatting
  http: ^1.1.0
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  firebase_auth_web: ^5.8.13

flutter:
  uses-material-design: true
  
  assets:
    - .env
    - assets/
